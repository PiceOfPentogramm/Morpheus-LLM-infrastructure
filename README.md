# Morpheus — LLM Infrastructure

Приложение запускается на компьютере пользователя (в виде **.exe**) и общается с пользователем через Telegram. Ядро и инфраструктура разделены и общаются между собой через локальный API.

## Что внутри

### Infrastructure (инфраструктура)
- Небольшое окно настройки (токен бота, выбор источника LLM, режим работы).
- Подключение к Telegram.
- Локальный API (связь с ядром).
- Подготовка к упаковке в .exe.

### Core (ядро)
- Получает запросы от инфраструктуры через API.
- Делает обработку и формирует ответ.
- Поддерживает разные источники LLM и режимы работы (описано ниже).

## Источники LLM (2 варианта)

1. **Внешняя LLM по токену**
   - Ядро отправляет запросы во внешний сервис (по API‑токену).
   - Подходит, когда нужна максимальная "сила" модели и есть интернет.

2. **Локальная LLM**
   - Модель работает прямо на ПК пользователя.
   - Подходит, когда важна приватность и автономность.

## Режимы работы ядра (2 режима)

1. **Прямой ввод → вывод (без обработки)**
   - Сообщение из Telegram уходит в LLM и ответ сразу возвращается пользователю.
   - Это самый простой режим для MVP и отладки.

2. **Полноценная обработка с трёхслойной памятью**
   - Перед запросом к LLM ядро подмешивает "память", чтобы ответы были умнее и стабильнее.
   - Три слоя памяти:
     - **Короткая**: контекст текущего диалога (последние 5-10 сообщений).
     - **Средняя**: важные факты текущей сессии (тема разговора, ключевые детали).
     - **Долгая**: профиль пользователя (имя, интересы, предпочтения).

## Структура проекта

```
Morpheus-LLM-infrastructure/
├── main.py                  # Точка входа
├── requirements.txt         # Зависимости
├── config/                  # Настройки
├── infrastructure/          # Инфраструктура
│   ├── api/                # Локальный API сервер
│   ├── gui/                # Окно настроек
│   └── telegram/           # Telegram бот
└── core/                    # Ядро приложения
    ├── core_manager.py     # Главный менеджер
    ├── llm_providers/      # Провайдеры LLM
    │   ├── external_llm.py # Внешний API
    │   └── local_llm.py    # Локальная модель
    ├── modes/              # Режимы работы
    │   ├── simple_mode.py  # Простой режим
    │   └── memory_mode.py  # Режим с памятью
    └── memory/             # Система памяти
        ├── short_memory.py  # Короткая память
        ├── medium_memory.py # Средняя память
        └── long_memory.py   # Долгая память
```

## Как это работает

1. Пользователь запускает .exe.
2. В окне настройки указывает:
   - Токен Telegram‑бота
   - Источник LLM (внешний/локальный)
   - Режим ядра (простой/с памятью)
3. Дальше всё общение идёт в Telegram.
4. Поток сообщений:
   ```
   Telegram → Infrastructure → (локальный API) → Core → (локальный API) → Infrastructure → Telegram
   ```

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `config/settings.py` с вашими настройками:

```python
class Settings:
    # Telegram
    BOT_TOKEN = "ваш-токен-бота"
    
    # API сервер
    API_PORT = 5000
    
    # Ядро
    LLM_SOURCE = "external"  # или "local"
    LLM_MODE = "simple"      # или "memory"
    
    # Для внешней LLM
    OPENAI_API_KEY = "ваш-ключ-openai"
    
    # Для локальной LLM
    LOCAL_MODEL_PATH = "models/llama.gguf"
```

### 3. Запуск

```bash
python main.py
```

## Текущий статус разработки

✅ **Готово:**
- Структура проекта
- Telegram бот с кнопкой регенерации
- Архитектура ядра (провайдеры, режимы, память)
- Система трёхслойной памяти

🚧 **В процессе:**
- Интеграция API сервера с ядром
- Окно настроек (GUI)
- Упаковка в .exe

📋 **Запланировано:**
- Полная интеграция локальной LLM
- Умное наполнение средней и долгой памяти
- Дополнительные функции бота

## Зависимости

- `pyTelegramBotAPI` - для работы с Telegram
- `flask` - для локального API сервера
- `openai` - для внешней LLM
- `requests` - для HTTP запросов

## Лицензия

Проект находится в разработке.
